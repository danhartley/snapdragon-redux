import { getGlossary } from 'api/glossary/glossary';

test('should concatenate requested glossaries', () => {
    // const requiredGlossaries = [ 'common', 'plantae' ];
    // const glossaries = getGlossary(requiredGlossaries);
    // expect(glossaries.length).toBeGreaterThan(0);
    // expect(glossaries.find(glossary => glossary.term === 'Entomophily').definition).toEqual("Pollination of plants, particularly flowering plants, by insects.");
})