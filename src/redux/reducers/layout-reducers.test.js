import * as R from 'ramda';

import { types } from 'redux/actions/action-types';
import { snapdragon } from 'test/redux-test-snapdragon/state-1';
// import { lessonPlanner } from 'snapdragon-engine/lesson-planner';
// import { lesson } from 'redux/reducers/lesson-reducers';

test('Level should move to the next when the previous one has completed', () => {
    // let state = R.clone(snapdragon.lessonPlan);
    // expect(state.levelName).toBe('Level 1');
    // const { updatedLesson } = lessonPlanner.createLessonPlan(snapdragon.lessonPlan, snapdragon.config, snapdragon.collection, snapdragon.lesson);
    // state = lesson(updatedLesson, { type: types.NEXT_LEVEL, data: 0 } );
    // expect(state.level.name).toEqual('Level 2');
  });
