import { lessonStateHandler} from 'ui/screens/lists/lesson-state-handler';

test('should return ', () => {
    
    let lessons, currentCollection, newCollection, config, counter;

    currentCollection = { id: 0 }; // default
    newCollection = { id: 1 } 

});